import 'package:flutter/widgets.dart';

const sizeS = 8.0;
const sizeM = 16.0;
const sizeL = 32.0;

const spaceS = SizedBox.square(dimension: sizeS);
const spaceM = SizedBox.square(dimension: sizeM);
const spaceL = SizedBox.square(dimension: sizeL);

const edgeAllS = EdgeInsets.all(sizeS);
const edgeAllM = EdgeInsets.all(sizeM);
const edgeAllL = EdgeInsets.all(sizeL);
